gcp:
  project-id: ${GCP_PROJECT_ID}

alpaca:
  base-url: https://paper-api.alpaca.markets
  ws-url: wss://stream.data.alpaca.markets/v2/iex

spring:
  cloud:
    gcp:
      pubsub:
        publisher:
          enable-message-ordering: true
